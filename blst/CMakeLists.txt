cmake_minimum_required(VERSION 2.6)

if(${CMAKE_HOST_SYSTEM_NAME} STREQUAL "Windows")
    if(${ARCH} STREQUAL "amd64" AND MSVC)
        enable_language(ASM_MASM)
        file(GLOB src_list "src/server.c" "src/asm/win64/*-x86_64.asm")
#    elseif(${ARCH} STREQUAL "arm64")
#        enable_language(ASM_MARMASM)
#        file(GLOB src_list "src/server.c" "src/asm/win64/*-armv8.asm")
    else()
        add_definitions(-D__BLST_NO_ASM__)
        file(GLOB src_list "src/server.c")
    endif()
else()
    if(${ARCH} STREQUAL "arm64" OR ${ARCH} STREQUAL "amd64" OR ${ARCH} STREQUAL "i386" OR ${ARCH} STREQUAL "arm")
        enable_language(ASM)
        file(GLOB src_list "src/server.c" "src/asm/assembly.S")
    else()
        add_definitions(-D__BLST_NO_ASM__)
        file(GLOB src_list "src/server.c")
    endif()
endif()

include(../build_tools/cmake/Library.cmake)
